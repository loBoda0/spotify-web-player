export const initialState = {
  token: null,
  playlists: [],
  userInfo: null,
  selectedPlaylistId: '3AjzU5c4NBT2UV63tiu67B',
  selectedPlaylist: null,
  currentlyPlaying: null,
  playerState: false
}

const reducer = (state, action) => {
  switch(action.type) {
    default:
      return state
    }
}

export default reducer